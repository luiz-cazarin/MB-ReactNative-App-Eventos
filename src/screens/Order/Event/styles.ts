import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    display: "flex",
  },
  divisor: {
    height: 8,
    backgroundColor: "#EFEFEF",
  },
  boxButton: {
    marginHorizontal: 20,
  },
});
