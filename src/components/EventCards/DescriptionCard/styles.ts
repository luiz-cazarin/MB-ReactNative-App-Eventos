import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 15,
    minHeight: 178,
  },
  title: {
    fontSize: 18,
    fontWeight: "600",
  },
  description: {
    paddingTop: 7,
    fontSize: 16,
  },
});
